# Claude Code Gateway - Environment Configuration
# Copy to .env and fill in your API keys

# Server
GATEWAY_PORT=8787
GATEWAY_HOST=127.0.0.1
GATEWAY_LOG_LEVEL=INFO

# Provider API Keys (only set keys for providers you use)
# Get keys from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-your-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api

# Get keys from: https://console.groq.com/keys
GROQ_API_KEY=gsk-your-key-here

# Get keys from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-key-here

# NVIDIA NIM (self-hosted or cloud)
# NIM_BASE_URL=http://localhost:8000
# NIM_API_KEY=your-nim-key

# Get keys from: https://console.mistral.ai/api-keys
MISTRAL_API_KEY=your-mistral-key

# Get keys from: https://cloud.cerebras.ai (free tier ~5 RPM, 1M tokens/day)
CEREBRAS_API_KEY=your-cerebras-key

# OpenCode Zen (opencode's own hosted models, OpenAI-compatible).
# Free models work with the shared public key; set your own for higher limits.
# Get a key from: https://opencode.ai
OPENCODE_API_KEY=public

# Discovery
DISCOVERY_CACHE_TTL=300
# DISCOVERY_CACHE_PATH=~/.claude/cache/gateway-models.json

# Timeouts
REQUEST_TIMEOUT=600
CONNECT_TIMEOUT=10
# Seconds of upstream silence before a streaming `ping` heartbeat is emitted
# (keeps slow reasoning models alive without killing the request). 0 disables.
KEEPALIVE_INTERVAL=15

# Rate Limiting (local enforcement for free tiers)
GEMINI_RPM=15
MISTRAL_RPM=500
CEREBRAS_RPM=5

# Fallback
MAX_FALLBACK_ATTEMPTS=3